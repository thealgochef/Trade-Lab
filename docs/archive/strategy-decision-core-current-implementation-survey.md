# Strategy Decision Core Current Implementation Survey

Current-state documentation only. This survey compares the strategy-decision pipeline in `Claude-Quant-Lab` (Research) and `Trade-Lab` using file:line evidence supplied from both repositories. It does not propose designs, fixes, refactors, or extraction steps.

## Repository and packaging snapshot

| Repo | Python/package evidence | Layout evidence | Packaging feasibility, standpoint only |
| --- | --- | --- | --- |
| Research / `Claude-Quant-Lab` | `pyproject.toml:5-10`: `[project] name = "alpha-signal-lab"`, `version = "0.1.0"`, `requires-python = ">=3.11"` | `pyproject.toml:49-50`: `[tool.setuptools.packages.find] where = ["src"]` | Python project using `src/` layout and installable packaging metadata. |
| Trade-Lab | `backend/pyproject.toml:5-20`: `[project] name = "trade-lab"`, `version = "0.1.0"`, `requires-python = ">=3.13"` | `backend/pyproject.toml:28-33`: hatch wheel packages `src/trade_lab`; pytest `testpaths = ["tests"]`; `pythonpath = ["src"]` | Python backend project using `src/` layout and wheel packaging metadata. |

Packaging feasibility (D): from a packaging standpoint only, both codebases are Python projects with `src/` layouts and package metadata. The Python version floors differ (`>=3.11` vs `>=3.13`). Dependencies also differ: Research includes pandas/numpy/duckdb/catboost/scikit-learn/sqlalchemy/asyncpg (`pyproject.toml:12-35`), while Trade-Lab backend includes catboost/fastapi/httpx/orjson/pyarrow/pydantic/uvicorn (`backend/pyproject.toml:5-20`). Trade-Lab also keeps Databento optional/lazy (`backend/src/trade_lab/adapters/databento.py:54-60`, `def is_databento_sdk_available() -> bool:`) and CatBoost lazy-loaded (`backend/src/trade_lab/services/model_registry.py:318-326`, `def _load_catboost(model_file: Path) -> CatBoostClassifier:`).

## Stage-by-stage side-by-side table

| stage | Research (file:line, signature, batch/stream, data type) | Trade-Lab (file:line, signature, batch/stream, data type) |
| --- | --- | --- |
| 1. Ingest | Batch parquet/DuckDB. `src/alpha_lab/agents/data_infra/tick_store.py:50-55`, `def __init__(self, data_dir: Path | str, read_only: bool = True, tick_filename: str | None = None) -> None:`. `tick_store.py:65-81`, `def register_symbol_date(self, symbol: str, dt: date | str) -> bool:` creates a DuckDB view via `read_parquet`. `tick_store.py:627-641`, `def _resolve_tick_path(self, symbol: str, date_str: str) -> Path | None:` chooses `mbp10.parquet`, `mbp1.parquet`, or `trades.parquet`. Data type: parquet rows queried to `pd.DataFrame`. Stream/dashboard ingest uses dataclasses in `src/alpha_lab/dashboard/pipeline/rithmic_client.py:29-49`: `TradeUpdate`, `BBOUpdate`. | Streaming live + batch replay. Live: `backend/src/trade_lab/adapters/databento.py:132-146`, `class DatabentoMarketDataFeed.__init__(..., trade_schema="trades", quote_schema="mbp-1", context_schemas=("definition","status","statistics"), ...)`; `databento.py:322-327`, `def normalize_provider_message(message: Any, *, requested_symbol: str, schema: str) -> MarketEvent:`. Replay: `backend/src/trade_lab/adapters/historical_parquet.py:103-111`, `def scan(...) -> Iterator[TradeEvent | TopOfBookEvent | DataQualityWarning]:`; dispatch at `historical_parquet.py:188-200`, MBP-10 projection at `historical_parquet.py:320-336`. Data type: canonical frozen domain events. |
| 2. Bar construction | Batch time bars: `tick_store.py:345-351`, `def build_bars_from_ticks(self, symbol: str, start: datetime, end: datetime, bar_size: str = "5 minutes") -> pd.DataFrame:` returns `[open, high, low, close, volume]` with `DatetimeIndex` at `tick_store.py:360-362`; query uses `time_bucket`, mid `(bid_px_00 + ask_px_00)/2.0`, OHLC/sum at `tick_store.py:400-419`. Batch tick bars: `tick_store.py:451-457`, `def build_tick_bars(..., tick_count: int = 987) -> pd.DataFrame:`; closure uses `ROW_NUMBER()`, `(rn // tick_count) AS bar_id`, and `HAVING COUNT(*) = tick_count` at `tick_store.py:510-533`. Dashboard selector: `dashboard_utility_builder.py:268-307`, `def _build_bars_for_date(...) -> pd.DataFrame:`. Stream tick bars: `src/alpha_lab/dashboard/pipeline/tick_bar_builder.py:97-127`, `def on_trade(self, trade: TradeUpdate) -> None:` emits `OHLCVBar`. | Streaming/domain tick bars. `backend/src/trade_lab/domain/candles.py:103-118`, `class CandleEngine.__init__(self, timeframes: tuple[int, ...] = (147, 987, 2000)) -> None`, `def process_event(self, event: MarketEvent) -> CandleUpdate:`. Non-trades snapshot only at `candles.py:118-123`; trade path closes when `candle.trade_count == timeframe` at `candles.py:123-172`. Batch/seed: `backend/src/trade_lab/services/seed.py:42-44`, `def build_tick_bars_from_frame(frame: "pd.DataFrame", timeframes: tuple[int, ...]) -> list[Candle]:`; close/partial logic at `seed.py:106-128`. Data type: `Candle`, `CandleUpdate`. |
| 3. Level/zone definition | Batch training: `dashboard_utility_builder.py:347-376`, `def _compute_levels_for_date(...) -> list[dict]:` adds PDH/PDL/Asia/London levels. Zone proximity: `dashboard_utility_builder.py:53-54`, `_ZONE_PROXIMITY = 3.0`. Zone grouping: `dashboard_utility_builder.py:382-411`, `def _build_zones(levels: list[dict]) -> list[dict]:` groups by proximity and representative mean. Experiment: `src/alpha_lab/experiment/key_levels.py:289-337` adds availability windows. Live/dashboard: `dashboard/engine/level_engine.py:60-93`, `def compute_levels(self, trading_date: date, current_time: datetime | None = None) -> list[KeyLevel]:`. Data types: dict zones, experiment `Zone`, live `KeyLevel`/`LevelZone`. | Streaming/session engine. `backend/src/trade_lab/domain/levels.py:16-25`, `LevelKind` includes PDH/PDL/ASIA/LONDON/NY high/low; `levels.py:27-42`, `SESSION_LEVELS`. `levels.py:189-202`, `def _level(self, kind: LevelKind, price_ticks: int, current_session: SessionName | None) -> DisplayLevel:` marks current-session levels developing and eligibility. Contract zone fields only: `backend/src/trade_lab/domain/contracts/strategy_contract.py:73-79`, `class TouchRule` has `zone_proximity_pts`, `zone_representative_price`, `scope`, `direction_from_side`; fixture at `backend/tests/fixtures/strategy.json:72-78`. No runtime zone domain object found. Data type: `DisplayLevel`; contract-only `TouchRule`. |
| 4. Touch detection | Batch training: `dashboard_utility_builder.py:414-416`, `def _detect_touches(bars_et: pd.DataFrame, zones: list[dict]) -> list[dict]:`; decision at `dashboard_utility_builder.py:420-438` uses bar high/low intersection and first touch per initialized zone. Experiment: `event_detection.py:173-177`, `def detect_touches_single_day(bars: pd.DataFrame, zones: list[Zone], date_str: str) -> list[dict]:`; decision at `event_detection.py:199-214` also checks `available_from`. Live: `dashboard/engine/touch_detector.py:78-80`, `def on_trade(self, trade: TradeUpdate) -> TouchEvent | None:`; decision at `touch_detector.py:94-121` uses active zones and trade price crossing. Data types: touch dicts or `TouchEvent`. | Streaming exact-price/session-level touch. `backend/src/trade_lab/domain/levels.py:99-121`, `class SessionLevelEngine`, `def process_trade(self, trade: TradeEvent) -> LevelUpdate:`. `levels.py:245-274`, `def _detect_touches(self, trade: TradeEvent, trading_day: date, session: SessionName) -> tuple[TouchEvent, ...]:` uses eligible levels, key `(trading_day, session, kind)`, skips touched, and requires `trade.price_ticks == level_price_ticks`. Eligible prices: `levels.py:276-292`, `def _eligible_level_prices(self, current_session: SessionName) -> tuple[tuple[LevelKind, int], ...]:`. Data type: domain `TouchEvent`. |
| 5. Session/time classification | Batch constants: `dashboard_utility_builder.py:44-54`, `_ET = "US/Eastern"`, Asia 18:00-01:00, London 01:00-08:00, NY RTH 09:30-16:15. Conversion: `dashboard_utility_builder.py:314-322`, `def _ensure_et_index(bars: pd.DataFrame) -> pd.DataFrame:`. Experiment: `experiment/labeling.py:233-244`, `def _classify_event_session(event_ts: pd.Timestamp) -> str`. Live: `dashboard/engine/touch_detector.py:41-58`, `def _classify_session(ts_utc: datetime) -> str`. Separate data-infra: `agents/data_infra/sessions.py:41-88`, `def tag_sessions(bars: pd.DataFrame, instrument: str) -> pd.DataFrame:` and `sessions.py:91-113`, `def classify_killzone(timestamp: pd.Timestamp) -> Killzone:`. | Domain CT classification. `backend/src/trade_lab/domain/sessions.py:8`, `CT = ZoneInfo("America/Chicago")`. `sessions.py:29-32`, `def to_ct(ts_utc: datetime) -> datetime:` requires tz-aware timestamps. `sessions.py:35-52`, `def classify_session(ts_utc: datetime) -> SessionInfo:` with CT windows: `>=18` Asia next day, `<2` Asia, `<8` London, `<16` NY, else closed. Data type: `SessionInfo`. |
| 6. Feature computation | Batch training feature names: `dashboard_utility_builder.py:37-42`, `DASHBOARD_FEATURES = ["int_time_beyond_level", "int_time_within_2pts", "int_absorption_ratio"]`. Signature: `dashboard_utility_builder.py:446-451`, `def _compute_interaction_features(touch: dict, data_dir: Path, symbol: str, config: DashboardUtilityConfig) -> dict[str, float] | None:`; query at `dashboard_utility_builder.py:474-480`; tempo at `491-508`; absorption at `511-530`; returns at `534-538`. Optional approach config: `src/alpha_lab/agents/data_infra/ml/config.py:17-35`; delegation at `dashboard_utility_builder.py:598-606`. Experiment: `experiment/features.py:372-380`, `def _query_interaction_features(...) -> dict[str, float]`; cross-window features at `experiment/features.py:571-615`. Live: `dashboard/engine/feature_computer.py:31-39`, `def compute_features(...) -> dict[str, float]:`, returns same three at `feature_computer.py:60-64`. | Streaming inference features. `backend/src/trade_lab/services/inference/features/feature_functions.py:128-131`, `FeatureFn = Callable[["MarketContextBuffer", FeatureWindow, LevelContext], float]`. `feature_functions.py:335-343`, `def build_feature_vector(buffer: MarketContextBuffer, level_ctx: LevelContext, contract: StrategyContract, *, registry: FeatureFunctionRegistry = DEFAULT_FEATURE_REGISTRY, touch_ts_utc: datetime | None = None, window: FeatureWindow | None = None) -> tuple[list[float], dict[str, float]]:`. FeatureWindow at `feature_functions.py:98-125`; registry at `feature_functions.py:323-331` includes three interaction and three approach features. Runtime trigger: `backend/src/trade_lab/services/runtime.py:304-337`, `_run_inference`. Data type: `tuple[list[float], dict[str, float]]`. |
| 7. Label/outcome | Batch training labels. `dashboard_utility_labeling.py:44-48`, `def label_touch_event(event: dict | pd.Series, forward_bars: pd.DataFrame, config: DashboardUtilityConfig) -> dict:`. MAE-first at `dashboard_utility_labeling.py:71-97`; returns `label`, `label_encoded`, `max_mfe`, `max_mae`, `resolution_ts`, `bars_to_resolution` at `dashboard_utility_labeling.py:101-108`. Forward bars filtered before RTH cutoff in `dashboard_utility_builder.py:215-224`. Experiment: `experiment/labeling.py:99-102`, `def label_single_event(event: dict | pd.Series, forward_bars: pd.DataFrame) -> dict:`, same MAE-first at `experiment/labeling.py:124-150`. Live dashboard: `dashboard/model/outcome_tracker.py:118-135` checks MFE before MAE. | Streaming outcomes. `backend/src/trade_lab/domain/outcomes.py:19-25`, `ResolutionType`; `outcomes.py:28-42`, `class Outcome`. `backend/src/trade_lab/services/inference/outcome_tracker.py:73-82`, `class OutcomeTracker.__init__(self, contract: StrategyContract) -> None:`. Policy parse at `outcome_tracker.py:82-90`. `outcome_tracker.py:100-116`, `def register(self, prediction: Prediction) -> None:` and `def on_bar_close(self, bar: Candle) -> tuple[Outcome, ...]:`. Streaming resolution at `outcome_tracker.py:116-158`; MAE-first `_classify` at `outcome_tracker.py:160-191`. |
| 8. Orchestration | Training UI: `scripts/ml_training_tab.py:921-942` builds `MLPipelineConfig(training_mode="dashboard_utility", dashboard_utility=DashboardUtilityConfig(...))` and calls `build_utility_dataset(...)`. Dataset entry: `dashboard_utility_builder.py:57-62`, `def build_utility_dataset(dates: list[str], data_dir: Path, config: MLPipelineConfig, progress_fn=None) -> pd.DataFrame:`; calls `_process_single_date` at `dashboard_utility_builder.py:111-115`; per-date sequence at `181-224`; feature rows at `228-255`. Training config at `scripts/ml_training_tab.py:1065-1089`; `run_walk_forward_training` call at `1110-1114`; trainer signature `src/alpha_lab/agents/data_infra/ml/model_trainer.py:46-51`, `def train(...) -> TrainedModel:`; CatBoost at `model_trainer.py:142-164`; save signature at `scripts/ml_training_tab.py:517-525`. | Runtime orchestration. Entry: `backend/src/trade_lab/services/runtime.py:381-432`, `def process_market_event(self, event: MarketEvent) -> RuntimeUpdate:`. Trade path: `runtime.py:492-534` appends context, processes candles/levels, refreshes observations, runs inference, tracks outcomes, starts observations from touches. Replay: `backend/src/trade_lab/services/replay.py:144-177` start/reset/task; `replay.py:221-228` scan; `replay.py:322-327` calls runtime. Live: `backend/src/trade_lab/services/live.py:151-195` start/feed/task; `live.py:214-227` iterates `feed.events` and calls runtime. Seed: `live.py:243-258`, `service.build_seed_bars` and `runtime.seed_closed_bars`; `backend/src/trade_lab/services/seed.py:166-186`, `def build_seed_bars(self) -> tuple[Candle, ...]:`. |

## A. Data types observed

### Research / Claude-Quant-Lab

- Raw historical data: parquet rows queried through DuckDB into pandas DataFrames. `tick_store.py:30-35` defines `_TICK_FILENAME = "mbp10.parquet"`, `_OHLCV_FILENAME_PATTERN = "ohlcv_{tf}.parquet"`, and `_TICK_FILENAMES = ["mbp10.parquet", "mbp1.parquet", "trades.parquet"]`.
- Batch bars: `pd.DataFrame` with `[open, high, low, close, volume]` and `DatetimeIndex` (`tick_store.py:345-362`).
- Live bars: `src/alpha_lab/dashboard/pipeline/price_buffer.py:39-48`, dataclass `OHLCVBar(timestamp, open, high, low, close, volume)` with Decimal prices.
- Live trades/BBO: `src/alpha_lab/dashboard/pipeline/rithmic_client.py:29-49`, frozen slots dataclasses `TradeUpdate` and `BBOUpdate` using `Decimal` prices.
- Training zones: `dashboard_utility_builder.py:382-411`, `def _build_zones(levels: list[dict]) -> list[dict]:`, dicts with representative price, names, side, touched.
- Experiment zones: `src/alpha_lab/experiment/event_detection.py:38-48`, `class Zone(zone_id, level_names, level_prices, representative_price, available_from, touched=False)`.
- Live dashboard levels/zones/touches: `src/alpha_lab/dashboard/engine/models.py:61-90`, `KeyLevel`, `LevelZone`, `TouchEvent`.
- Feature rows: `dashboard_utility_builder.py:235-262` builds row dicts with event metadata, label fields, feature dict, then returns `pd.DataFrame(rows)`.
- Labels: `dashboard_utility_labeling.py:101-108` returns dict fields `label`, `label_encoded`, `max_mfe`, `max_mae`, `resolution_ts`, `bars_to_resolution`.
- Dashboard DB observation: `src/alpha_lab/dashboard/db/models.py:132-160`, `class ObservationEvent(Base)` includes prediction/outcome/MFE/MAE fields.

### Trade-Lab

- Canonical market events: `backend/src/trade_lab/domain/events.py:54-67`, frozen `TradeEvent`; `events.py:83-92`, `TopOfBookEvent`; `events.py:148-154`, `MarketEvent = TradeEvent | TopOfBookEvent | InstrumentDefinitionEvent | MarketStatusEvent | DailyStatisticEvent`.
- Bars: `backend/src/trade_lab/domain/candles.py:16-33`, `class Candle` with `timeframe_ticks`, `trading_day`, `bar_index`, `bar_id`, timestamps, OHLC ticks, volume/trade count, completion state, close reason; `candles.py:97-100`, `class CandleUpdate(completed, current)`.
- Levels/touches: `backend/src/trade_lab/domain/levels.py:45-53`, `class DisplayLevel`; `levels.py:55-69`, `class TouchEvent`.
- Runtime zone object: absent in runtime domain; zone/proximity appears in contract only (`strategy_contract.py:73-79`).
- Market context: `backend/src/trade_lab/domain/market_context.py:26-43`, `BufferedTrade`, `BufferedQuote`.
- Feature output: `backend/src/trade_lab/services/inference/features/feature_functions.py:335-343`, `def build_feature_vector(...) -> tuple[list[float], dict[str, float]]:`.
- Prediction: `backend/src/trade_lab/services/inference/inference_engine.py:40-59`, `class Prediction`.
- Outcome: `backend/src/trade_lab/domain/outcomes.py:28-42`, `class Outcome`.

## B. Isolatable vs tangled verdict per decision

| Decision | Research verdict and tight snippet | Trade-Lab verdict and tight snippet |
| --- | --- | --- |
| Provider/file ingest normalization | Isolatable. File choice is contained in `tick_store.py:627-641`, `def _resolve_tick_path(...) -> Path | None:` and filename constants include `"mbp10.parquet"`, `"mbp1.parquet"`, `"trades.parquet"` (`tick_store.py:30-35`). | Isolatable. `databento.py:322-327`, `def normalize_provider_message(...) -> MarketEvent:`; dispatch maps trade schemas to `TradeEvent` (`databento.py:334-346`), quote schemas to `TopOfBookEvent` (`databento.py:347-361`), context at `362-384`. Historical scan is isolated at `historical_parquet.py:103-111`. |
| Trades vs quotes affecting bars/touches | Mostly isolatable by path. Research training bars use tick queries and live `tick_bar_builder.py:97-127`, `def on_trade(self, trade: TradeUpdate) -> None:`; BBO remains separate in `BBOUpdate`. | Isolatable. Runtime dispatch at `runtime.py:381-392`; candles ignore non-trades: `candles.py:118-123`, non-`TradeEvent` returns snapshot. |
| Tick-bar close | Isolatable. Batch uses `HAVING COUNT(*) = tick_count` (`tick_store.py:510-533`); live closes when `acc.count >= acc.tick_count` (`tick_bar_builder.py:97-127`). | Isolatable. `candles.py:169-171` closes when `candle.trade_count == timeframe`; seed mirrors complete/partial close in `seed.py:106-128`. |
| Session classification | Duplicated but separable. Constants are local in `dashboard_utility_builder.py:44-54`; conversion is `def _ensure_et_index(...)` (`314-322`). Other classifiers exist in experiment/live/data-infra. | Isolatable. `sessions.py:35-52`, `def classify_session(ts_utc: datetime) -> SessionInfo:` centralizes CT session windows after `to_ct` (`sessions.py:29-32`). |
| Level eligibility | Duplicated across paths. Training groups levels in `_build_zones` (`dashboard_utility_builder.py:382-411`) after `_compute_levels_for_date` (`347-376`); experiment/live have separate level engines. | Isolatable. `levels.py:189-202`, `def _level(...) -> DisplayLevel:` sets developing/eligible; `levels.py:276-292`, `def _eligible_level_prices(...)`. |
| Touch detection rule | Tangled across duplicate implementations. Training snippet: `if bar_low <= rep <= bar_high` then `zone["touched"] = True`, `direction = "LONG" if zone["side"] == "LOW" else "SHORT"` (`dashboard_utility_builder.py:420-438`). Experiment adds `if bar_ts < zone.available_from: continue` (`event_detection.py:199-214`). Live uses trade crossing and session-local state (`touch_detector.py:94-121`). | Tangled/contract-mismatched. Runtime touch uses exact price/session-level: `key = (trading_day, session, kind)`, skip if key in `_touched`, require `trade.price_ticks == level_price_ticks` (`levels.py:245-274`). Contract/fixture says zone/proximity/bar intersect: `strategy_contract.py:73-79`, `zone_proximity_pts`, `zone_representative_price`, `scope`; `backend/tests/fixtures/strategy.json:72-78`, `bar_intersect`, `zone_proximity_pts: 3.0`, `scope: first_touch_per_zone_per_day`. |
| First-touch scope | Training scope is per processed date because zones initialize per `_process_single_date()`; touch loop skips `zone["touched"]` (`dashboard_utility_builder.py:420-438`). Experiment similar with `zone.touched`; live has RTH/non-RTH spending. Verdict: duplicated/tangled by path. | Tangled/contract-mismatched. Runtime key is `(trading_day, session, kind)` at `levels.py:250`; comment states one touch per level per session (`levels.py:217-221`), while fixture states first touch per zone per day (`backend/tests/fixtures/strategy.json:72-78`). |
| Feature computation | Duplicated but mostly separable by path. Training returns three names at `dashboard_utility_builder.py:534-538`; live returns same three at `feature_computer.py:60-64`; experiment includes larger matrix at `experiment/features.py:571-615`. | Isolatable. `FeatureFn = Callable[...]` at `feature_functions.py:128-131`; registry at `feature_functions.py:323-331`; vector builder at `feature_functions.py:335-343`. Tight snippets: beyond LONG mid below/SHORT mid above (`170-188`), within `abs(mid-level)<=_WITHIN_BAND_PTS` (`191-204`), absorption ratio (`217-239`). |
| Prediction eligibility | Training eligibility is implicit in row creation after labels/features (`dashboard_utility_builder.py:228-262`) and feature-column selection (`scripts/ml_training_tab.py:173-180`). | Isolatable. `inference_engine.py:175-182` contains prediction eligibility; prediction uses `predict_for_observation` (`inference_engine.py:135-148`) and feature/model path (`156-173`). |
| Outcome resolution | Training/experiment isolatable but duplicated. MAE-first snippets: `dashboard_utility_labeling.py:71-97` and `experiment/labeling.py:124-150`; live dashboard differs with MFE-first at `dashboard/model/outcome_tracker.py:118-135`. | Isolatable. `outcome_tracker.py:116-158` handles streaming resolution; `_classify` checks `max_mae_pts >= sl` before `max_mfe_pts >= tp` at `outcome_tracker.py:160-191`. |
| End-to-end runtime sequencing | Training orchestration is localized to UI + builder + trainer but spans `scripts/ml_training_tab.py`, `dashboard_utility_builder.py`, and `model_trainer.py`. | Tangled. `runtime.py:492-534` performs append trade context, candles, levels, observations, inference, outcomes, and starts observations from touches in one trade path. |

## C. Duplicate implementations found

### Claude-Quant-Lab duplicates

- Touch detection:
  - Training path: `dashboard_utility_builder.py:414-440`, `def _detect_touches(...)`; bar high/low intersection; first touch per zone for processed date.
  - Experiment path: `experiment/event_detection.py:173-236`, `def detect_touches_single_day(...)`; bar intersection plus `available_from`.
  - Dashboard/live path: `dashboard/engine/touch_detector.py:78-137`, `def on_trade(...)`; trade-price crossing, active zones, RTH/non-RTH handling.
  - Agreement: HIGH maps to SHORT and LOW maps to LONG. Difference: bar intersection vs trade crossing, availability gating, and RTH/non-RTH touch spending.
- Session classification:
  - Training path: `dashboard_utility_builder.py:44-51,332-344`.
  - Experiment path: `experiment/labeling.py:233-244`.
  - Dashboard/live path: `dashboard/engine/touch_detector.py:41-58`.
  - Data-infra path: `agents/data_infra/sessions.py:41-127`.
  - Agreement: broad Asia/London/NY boundaries mostly align. Difference: casing, pre/post-market naming, and separate killzone windows.
- Levels/zones:
  - Training path: `dashboard_utility_builder.py:347-411`.
  - Experiment path: `experiment/key_levels.py:227-346` and `experiment/event_detection.py:50-105`.
  - Dashboard/live path: `dashboard/engine/level_engine.py:60-293`.
  - Agreement: PDH/PDL, Asia, London, and proximity `3.0`. Difference: availability windows and mixed-zone side handling.
- Features:
  - Training path: `dashboard_utility_builder.py:446-538`.
  - Experiment path: `experiment/features.py:372-549` plus cross-window features at `571-615`.
  - Dashboard/live path: `dashboard/engine/feature_computer.py:31-169`.
  - Agreement: dashboard batch/live share the three interaction names. Difference: data sources and experiment’s larger matrix.
- Label/outcome:
  - Training path: `dashboard_utility_labeling.py:44-108`.
  - Experiment path: `experiment/labeling.py:99-158`.
  - Dashboard/live path: `dashboard/model/outcome_tracker.py:59-189`.
  - Agreement: training and experiment are MAE-first. Difference: live dashboard is MFE-first/favorable-wins.

### Trade-Lab duplicates or absent counterparts

- Touch rule counterpart mismatch: runtime touch is exact session-level (`levels.py:245-274`), while the strategy contract/fixture carries zone/bar-intersection/proximity fields (`strategy_contract.py:73-79`; `backend/tests/fixtures/strategy.json:72-78`).
- Batch seed and streaming candle construction both exist: streaming `CandleEngine.process_event/process_trade` (`candles.py:103-172`) and seed `build_tick_bars_from_frame` (`seed.py:42-128`). They agree on tick-count completion and partial end-of-day handling is seed-specific.
- Runtime zone grouping/proximity implementation: absent; only contract fields found.

## Orchestration call graphs

### Claude-Quant-Lab: one dashboard-utility training run

1. UI builds config and dataset request: `scripts/ml_training_tab.py:921-942` creates `MLPipelineConfig(training_mode="dashboard_utility", dashboard_utility=DashboardUtilityConfig(...))` and calls `build_utility_dataset(dates_in_range, data_dir, build_config, progress_fn=...)`.
2. Dataset entry: `dashboard_utility_builder.py:57-62`, `def build_utility_dataset(dates: list[str], data_dir: Path, config: MLPipelineConfig, progress_fn=None) -> pd.DataFrame:`.
3. Date loop: `dashboard_utility_builder.py:111-115` calls `_process_single_date(...)`.
4. Per-date pipeline (`dashboard_utility_builder.py:181-224`): `_build_bars_for_date` -> `_ensure_et_index` -> `_compute_levels_for_date` -> `_build_zones` -> `_detect_touches` -> forward bars -> `label_touch_event`.
5. Feature rows (`dashboard_utility_builder.py:228-255`): `_compute_interaction_features`; optional approach features via `alpha_lab.experiment.features` (`dashboard_utility_builder.py:598-606`); row dicts become `pd.DataFrame(rows)` (`235-262`).
6. Training config: `scripts/ml_training_tab.py:1065-1089`; call to `run_walk_forward_training` at `scripts/ml_training_tab.py:1110-1114`.
7. Feature columns: `scripts/ml_training_tab.py:173-180`; dashboard-utility uses columns starting with `int_` or `app_`.
8. Trainer: `src/alpha_lab/agents/data_infra/ml/model_trainer.py:46-51`, `def train(self, features: pd.DataFrame, y: pd.Series, cv_splits: list[tuple[np.ndarray, np.ndarray]] | None = None) -> TrainedModel:`; CatBoost builder at `model_trainer.py:142-164`.
9. Final refit and save: `scripts/ml_training_tab.py:398-402`; `scripts/ml_training_tab.py:517-525`, `def save_trained_model(...) -> Path:`.

### Trade-Lab: replay path

1. Replay start/reset/task: `backend/src/trade_lab/services/replay.py:144-177`.
2. Source scan: `replay.py:221-228` scans configured historical source.
3. Event processing: `replay.py:322-327` calls `self.runtime.process_market_event(item)`.
4. Runtime dispatch: `backend/src/trade_lab/services/runtime.py:381-432`, `def process_market_event(self, event: MarketEvent) -> RuntimeUpdate:`.
5. Trade path: `runtime.py:492-534` appends trade context, processes candles, processes levels/touches, refreshes observations, runs inference, tracks outcomes, starts observations from touches, returns `RuntimeUpdate`.
6. Quote path: `runtime.py:480-490` appends quote context.
7. Inference subgraph: observations start at `runtime.py:510-511`; expiration in `backend/src/trade_lab/domain/observations.py:63-76`; prediction via `inference_engine.py:135-148`, `def predict_for_observation(self, observation: Observation, buffer: MarketContextBuffer) -> Prediction | None:`; feature/model path at `inference_engine.py:156-173`.

### Trade-Lab: live path

1. Live start/reset/feed/task: `backend/src/trade_lab/services/live.py:151-195`.
2. Optional seed task: `live.py:190-195`; seed call at `live.py:243-258`, `service.build_seed_bars` and `runtime.seed_closed_bars`.
3. Seed service: `backend/src/trade_lab/services/seed.py:166-186`, `def build_seed_bars(self) -> tuple[Candle, ...]:` fetches trades frame and calls `build_tick_bars_from_frame`.
4. Feed event loop: `live.py:214-227` iterates `feed.events` and calls runtime.
5. Runtime dispatch and trade/quote paths are the same as replay: `runtime.py:381-432`, `runtime.py:480-490`, `runtime.py:492-534`.

## Explicit absences

- Trade-Lab runtime zone domain object: absent from findings; zone/proximity appears in `StrategyContract.TouchRule` and fixture only.
- Trade-Lab runtime bar-intersection touch rule: absent from findings; runtime touch requires exact `trade.price_ticks == level_price_ticks`.
- Research single shared decision-core implementation across training, experiment, dashboard/live: absent; duplicates are listed above.
